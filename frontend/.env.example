# Public API base URL for the deployed backend.
# Local default is http://127.0.0.1:8000 when unset.
# For Appwrite Sites deployment, set to the public HTTPS backend URL (e.g., https://api.example.com).
VITE_API_BASE_URL=
