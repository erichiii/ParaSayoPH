# Mock Contract Examples

These files are derived from `tests/test_public_programs.py` fixtures and represent the validated public response shape after DTO serialization. They are not Supabase-backed API samples and must not be presented as real programs.

- `programs-list.mock.json` represents `GET /programs`.
- `program-detail.mock.json` represents `GET /programs/17`.
